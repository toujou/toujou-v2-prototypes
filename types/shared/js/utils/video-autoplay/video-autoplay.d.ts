/**
 * Initializes autoplay or control behavior for videos with the `data-is-autoplay="1"` attribute,
 * based on the user's `prefers-reduced-motion` setting.
 */
declare function initAutoplayVideos(): void;

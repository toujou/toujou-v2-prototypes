# &lt;hero-media&gt;
The `hero-media` element renders media with a headline in a 3 / 1 format.

If the element is linked, the whole element is clickable

Example:
```html
<a href="#" class="hero-media" headline-alignment="center">
    <img src="https://picsum.photos/1200/1200" alt="beautiful image" class="hero-media__media">
    <h3 class="hero-media__title font--alpha">Hero Media</h3>
</a>
```

## Attributes

The `hero-media` element has some attributes that allow us to configure how it is rendered

### headline-alignment
This attribute allows us to define how to align the headline inside of the element

Valid values are `center`, `right` and `left`
